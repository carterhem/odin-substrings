def substring(string, dictionary)
  # handle any capitals in string: to lowercase
  # loop through words in dictionary
  # check if any of them match string OR include string
  # use reduce to create/puts a hash
end